version: '3.8'

services:
  lexcounsel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lexcounsel-app
    ports:
      - "3333:3333"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
